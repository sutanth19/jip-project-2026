import { AppError } from "../../errors/app-error.js";
const unsafe = /<\s*script|<\s*\/?\s*html|javascript\s*:|https?:\/\/[^\s]+\.(?:js|exe|sh)|ignore (?:all|previous) instructions|diagnos(?:e|is)|bunuh diri|seksual|pornografi|benci(?:an)?|bodoh|malas/i;
export function assertSafeAiText(value: string): void { if (unsafe.test(value) || /(?:__proto__|constructor\s*\[)/i.test(value)) throw new AppError("AI_CONTENT_BLOCKED", 422, "Kandungan AI tidak selamat untuk digunakan."); }
export function assertSafeAiJson(value: unknown, depth = 0): void { if (depth > 12) throw new AppError("AI_OUTPUT_INVALID", 422, "Output AI terlalu kompleks."); if (typeof value === "string") return assertSafeAiText(value); if (Array.isArray(value)) return value.forEach((entry) => assertSafeAiJson(entry, depth + 1)); if (value && typeof value === "object") for (const [key, entry] of Object.entries(value)) { if (key === "__proto__" || key === "constructor" || key === "prototype") throw new AppError("AI_OUTPUT_INVALID", 422, "Output AI tidak sah."); assertSafeAiJson(entry, depth + 1); } }
