import { CheckCircle2, RotateCcw } from "lucide-react"
import { Card, CardContent } from "@/components/ui/card"
export function ReadingFeedback({ show, complete, message }: { show: boolean; complete: boolean | null; message: string | null }) { if (!show || !message) return null; return <Card aria-live="polite" className={complete ? "border-secondary/40 bg-secondary/10" : "border-primary/30 bg-primary/5"}><CardContent className="flex gap-3 p-4">{complete ? <CheckCircle2 className="size-6 shrink-0 text-secondary" aria-hidden="true" /> : <RotateCcw className="size-6 shrink-0 text-primary" aria-hidden="true" />}<p className="font-semibold">{message}</p></CardContent></Card> }
