import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { ReadingOption } from "./ReadingOption"
import type { ReadingComprehensionQuestion, ReadingComprehensionQuestionState, ReadingComprehensionSettings } from "./reading.types"

export function ReadingQuestionCard({ question, state, settings, showQuestionNumber, onSelectOption, onTextChange, onSubmit }: { question: ReadingComprehensionQuestion; state: ReadingComprehensionQuestionState; settings: ReadingComprehensionSettings; showQuestionNumber: string; onSelectOption: (optionId: string) => void; onTextChange: (value: string) => void; onSubmit: () => void }) {
  return <Card className="border-border/80 shadow-sm"><CardHeader className="space-y-2"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-primary/10 px-3 py-1 text-sm font-semibold text-primary">{showQuestionNumber}</span><span className="rounded-full bg-secondary/10 px-3 py-1 text-sm font-semibold text-secondary">{question.type.replaceAll("_", " ")}</span></div><CardTitle className="text-lg sm:text-xl">{question.question}</CardTitle>{question.explanation ? <p className="text-sm text-muted-foreground">{question.explanation}</p> : null}</CardHeader><CardContent className="space-y-4">{question.type === "SHORT_TEXT" ? <Input value={state.textAnswer} aria-label={question.question} onChange={(event) => onTextChange(event.target.value)} /> : <div className="space-y-2">{question.options.map((option) => <ReadingOption key={option.id} option={option} selected={state.selectedOptionId === option.id} onSelect={() => onSelectOption(option.id)} />)}</div>}<button type="button" className="inline-flex h-11 items-center justify-center rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground" onClick={onSubmit}>Semak Jawapan</button>{state.validationMessage ? <p role="alert" className="text-sm font-medium text-destructive">{state.validationMessage}</p> : null}{state.feedback && state.submitted && settings.showImmediateFeedback ? <p aria-live="polite" className={`text-sm font-semibold ${state.isCorrect ? "text-secondary" : "text-primary"}`}>{state.feedback}</p> : null}</CardContent></Card>
}
